# 농협 비료/농약/자재 상품 웹 생성 AI 계획서

## 프로젝트 개요

**주제:** 취급상품 정보 기반 AI 대화형 웹페이지 자동 생성 시스템

**핵심 가치:**

> 농협 직원이 Excel만 업로드하면, AI가 데이터를 분석하고 대화를 통해 고객용/관리자용 맞춤 웹페이지를 자동 생성하며, QR코드로 현장 배포까지 지원

---

## 생성형 AI 활용 포인트

| AI 기능                  | 설명                                            | 활용 기술  |
| ------------------------ | ----------------------------------------------- | ---------- |
| Excel 구조 해석          | 불규칙한 컬럼명도 AI가 자동 파악                | Claude API |
| 상품 자동 분류           | 상품명 기반 카테고리 자동 태깅 (비료/농약/자재) | Claude API |
| 판매내역 분석            | 판매 데이터 기반 주요 작물 자동 추출            | Claude API |
| 페이지 설계도(JSON) 생성 | 대화 맥락 기반 페이지 구조 전체 생성            | Claude API |
| 섹션 카피 생성           | 배너 문구, 섹션 제목 자동 생성                  | Claude API |
| 자연어 수정 반영         | 관리자 자연어 요청 → 페이지 설정 변경           | Claude API |

---

## 핵심 아키텍처: AI JSON 생성 → React 렌더링

```
기존 방식: React 템플릿(고정) + 데이터 삽입 → 항상 같은 구조
새 방식:  AI → JSON 설계도 생성 → React가 JSON 읽고 렌더링 → 매번 다른 구조
```

### AI가 생성하는 JSON 예시

```json
{
  "store": {
    "name": "농협 OO지점",
    "theme": "green",
    "banner": "2025 봄 파종 시즌 특가"
  },
  "sections": [
    {
      "id": "featured",
      "type": "highlight-banner",
      "title": "이달의 추천 상품",
      "products": ["P001", "P003"]
    },
    {
      "id": "rice",
      "type": "card-grid",
      "title": "벼 재배 관련 상품",
      "products": ["P002", "P005", "P007"]
    }
  ],
  "hiddenProducts": ["P004", "P009"],
  "showPrice": false,
  "contact": "031-000-0000"
}
```

---

## 전체 서비스 흐름

```
[1단계] Excel 업로드
  - 취급상품 정보대장 업로드 (단가 포함)
  - AI: Excel 구조 자동 파악 + 컬럼 매핑
  - AI: 상품 카테고리 자동 분류 (비료/농약/자재)
  - 재고 있는 품목만 자동 필터링

[2단계] AI 대화 - 데이터 구성 (정해진 질문 순서)

  Q1. 숨길 상품 선택
      → 검색 + 체크박스 테이블 UI
      → AI: 카테고리 일괄 숨김 처리 가능

  Q2. 주요 작물 확인
      → AI가 판매내역 분석 → 주요 작물 자동 추출
         (예: "벼 전용 제초제 다수 판매" → "벼 재배 지역으로 판단")
      → 사용자 추가 입력 가능
      → 작물 선택 → 관련 품목 자동 매핑 확인/수정

  Q3. 디자인 방향 설정
      → 미리보기 카드 선택 (그린/화이트/컬러 테마)
      → 자유 텍스트로 세부 조정 가능
      → AI: 선택 기반 CSS + 레이아웃 생성

  Q4. 추가 정보 설정
      → 단가 공개 여부
      → 연락처/주소 표시 여부
      → 배너 문구 (AI 자동 생성 or 직접 입력)

[3단계] AI 페이지 설계도(JSON) 생성
  - 모든 입력값 종합
  - 섹션 구조, 순서, 레이아웃 타입 AI가 결정
  - 관리자용 / 고객용 JSON 분리 생성

[4단계] 페이지 생성 및 배포
  - React가 JSON 읽고 렌더링
  - 관리자용 URL (단가 + 전체 상품 포함)
  - 고객용 URL (공개 설정 기준)
  - QR코드 자동 발급
```

---

## 관리자용 vs 고객용 분리

| 항목      | 관리자용         | 고객용      |
| --------- | ---------------- | ----------- |
| 단가      | 표시             | 설정에 따라 |
| 숨긴 상품 | 표시 (관리 목적) | 미표시      |
| 재고 수량 | 표시             | 미표시      |
| 접근 제한 | 비밀번호 보호    | 공개        |
| QR코드    | 관리자용 QR      | 고객용 QR   |

---

## 기술 스택

| 역할          | 기술                           |
| ------------- | ------------------------------ |
| 프론트엔드    | React + Tailwind CSS           |
| 디자인 시스템 | shadcn/ui                      |
| AI            | Claude API (claude-sonnet-4-6) |
| Excel 파싱    | xlsx 라이브러리                |
| 데이터베이스  | Supabase                       |
| 배포          | Vercel                         |
| QR코드        | qrcode.js                      |

---

## 경쟁력 요약

1. **AI가 진짜 설계** — 페이지 구조/순서/레이아웃을 AI가 결정 (템플릿 채우기 아님)
2. **판매 데이터 인사이트** — 판매내역으로 지역 주요 작물 자동 파악
3. **자연어 인터페이스** — 코딩 없이 자연어 대화로 페이지 생성/수정
4. **이중 페이지 시스템** — 고객용/관리자용 자동 분리
5. **현장 배포** — QR코드로 포장지/매장에서 즉시 활용

---

## 심사위원 어필 포인트

> "농협 직원이 Excel 하나만 업로드하면,
> AI가 판매 데이터를 분석해 지역 주요 작물을 파악하고,
> 대화를 통해 페이지 구조 전체를 설계한 뒤,
> 고객용/관리자용 웹페이지와 QR코드를 자동으로 생성합니다."

---

## 작성일

2026-06-02
