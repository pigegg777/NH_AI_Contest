name = "react-app"
# pages_build_output_dir = "dist"  # commented out for local `wrangler pages dev --proxy` testing; actual deploys use the Cloudflare dashboard's Build output directory setting instead
compatibility_date = "2026-06-22"
